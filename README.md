# UTD -> Universal Terminal Debugger
This project aims to become a universal debugger usable from terminal relying
on the DAP protocol.

## Build instructions
```bash
go build -o bin /cmd/utd/utd.go
```
